# ===========================================
# ENVIRONMENT CONFIGURATION
# ===========================================
# Copy this file to .env.local for local development
# Copy this file to .env.production for production

# ===========================================
# DATABASE CONNECTION
# ===========================================

# LOCAL DEVELOPMENT (PostgreSQL lokal)
# DATABASE_URL="postgresql://postgres:password@localhost:5432/korjotirto_dev"

# PRODUCTION (Supabase atau PostgreSQL production)
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# ===========================================
# INSTRUCTIONS
# ===========================================
# 1. Buat 2 file:
#    - .env.local   (untuk development, DATABASE_URL ke database lokal)
#    - .env         (untuk production, DATABASE_URL ke Supabase/production DB)
#
# 2. Next.js akan otomatis load:
#    - .env.local jika ada (prioritas lebih tinggi)
#    - .env sebagai fallback
#
# 3. Di Vercel, set DATABASE_URL di Environment Variables
#    (Vercel tidak menggunakan file .env)
